import type { Worker, WorkerAssignment, EmploymentHistory } from '../../types';

export const mockWorkers: Worker[] = [];
export const mockWorkerAssignments: WorkerAssignment[] = [];
export const mockEmploymentHistory: EmploymentHistory[] = [];
